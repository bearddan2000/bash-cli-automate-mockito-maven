# bash-cli-automate-mockito-maven

## Description
Automates adding `mockito` dependency.
Uses python `xml.etree` lib to add the plugin
and reformat the xml.

## Tech stack
- bash
- python

## To run
`./install.sh`
